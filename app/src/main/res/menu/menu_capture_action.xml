<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item
        android:id="@+id/action_download"
        android:icon="@drawable/ic_baseline_download_24"
        app:iconTint="?attr/colorOnSurface"
        app:showAsAction="ifRoom"
        android:title="@string/download"/>

    <!-- Copy every selected capture's URL, one per line (issue #302). -->
    <item
        android:id="@+id/action_copy_url"
        android:icon="@drawable/ic_copy_24"
        app:iconTint="?attr/colorOnSurface"
        app:showAsAction="ifRoom"
        android:title="@string/capture_copy_url"/>

    <item
        android:id="@+id/action_select_all"
        android:icon="@drawable/select_all_24"
        app:iconTint="?attr/colorOnSurface"
        app:showAsAction="ifRoom"
        android:title="@string/select_all"/>

    <item
        android:id="@+id/action_select_all_not"
        android:icon="@drawable/deselect_24"
        app:iconTint="?attr/colorOnSurface"
        app:showAsAction="ifRoom"
        android:title="@string/select_all_not"/>

</menu>