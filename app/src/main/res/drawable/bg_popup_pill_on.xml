<?xml version="1.0" encoding="utf-8"?>
<!--
    Active-state variant of bg_popup_pill — uses the primary container
    tone so an "On" pill reads as enabled at a glance, paired with
    colorOnPrimaryContainer text. Swapped against bg_popup_pill (the
    neutral / off state) at runtime by the popup fragment.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorPrimaryContainer" />
    <corners android:radius="999dp" />
</shape>
