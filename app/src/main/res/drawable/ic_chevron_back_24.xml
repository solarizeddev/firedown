<?xml version="1.0" encoding="utf-8"?>
<!-- Thin left chevron for the browser popup Back button (matches the design
     render's stroked ‹ rather than the chunky filled arrow ic_arrow_back_24).
     strokeColor is a placeholder — the quick-row button's iconTint recolors it
     (incl. the disabled-state alpha). autoMirrored so it flips in RTL. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:autoMirrored="true">
    <path
        android:strokeColor="#FFFFFF"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M15,6 L9,12 L15,18" />
</vector>
