<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item_web_history_holder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/item_web_selected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="@dimen/list_spacing"
        android:visibility="gone"
        app:srcCompat="@drawable/ic_baseline_check_circle_24" />

    <Space
        android:id="@+id/spacer"
        android:layout_width="@dimen/list_spacing"
        android:layout_height="match_parent"
        android:visibility="gone"/>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/item_web_bookmark"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        app:cardCornerRadius="8dp"
        app:strokeWidth="2dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="8dp">

            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/file_icon"
                android:layout_width="@dimen/list_web_image_height"
                android:layout_height="@dimen/list_web_image_height"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:contentDescription="@null"
                android:scaleType="fitCenter"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"/>


            <!--
                Pin badge — small overlay on the favicon's bottom-right
                corner, visible only when WebBookmarkAdapter sets
                isPinned. Stays out of the title's column entirely so
                pinned vs unpinned rows have identical text geometry;
                the favicon carries the status marker the way a
                notification dot rides on an app icon.
            -->
            <!--
                Pin badge — small status marker just before the
                trailing more-menu button. WebBookmarkAdapter toggles
                visibility; when GONE, ConstraintLayout collapses it
                to a 0-size anchor and file_name's end snaps directly
                to file_more, so unpinned rows render at full title
                width. Pinned rows show the badge between title and
                more, signalling status without taking a column at
                the start of the row.
            -->
            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/file_pin_badge"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginEnd="8dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_push_pin_filled_16"
                android:visibility="gone"
                app:tint="?attr/colorOnSurfaceVariant"
                app:layout_constraintEnd_toStartOf="@id/file_more"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                tools:visibility="visible"
                tools:ignore="ContentDescription" />

            <TextView
                android:id="@+id/file_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_marginTop="4dp"
                android:layout_marginEnd="12dp"
                android:ellipsize="marquee"
                android:singleLine="true"
                style="@style/TextAppearance.Material3.TitleMedium"
                app:layout_constraintEnd_toStartOf="@id/file_pin_badge"
                app:layout_constraintStart_toEndOf="@id/file_icon"
                app:layout_constraintTop_toTopOf="parent"/>

            <TextView
                android:id="@+id/file_url"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_below="@id/file_name"
                android:layout_marginStart="12dp"
                android:layout_marginTop="2dp"
                android:layout_marginEnd="12dp"
                android:layout_marginBottom="12dp"
                android:ellipsize="marquee"
                android:singleLine="true"
                style="@style/TextAppearance.Material3.BodySmall"
                app:layout_constraintStart_toEndOf="@id/file_icon"
                app:layout_constraintEnd_toStartOf="@id/file_pin_badge"
                app:layout_constraintTop_toBottomOf="@id/file_name"/>

            <androidx.appcompat.widget.AppCompatImageButton
                android:id="@+id/file_more"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:layout_gravity="end"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:nextFocusLeft="@id/item_web_bookmark"
                android:padding="6dp"
                android:src="@drawable/ic_baseline_more_vert_24"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>

</LinearLayout>