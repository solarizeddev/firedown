<?xml version="1.0" encoding="utf-8"?>
<!--
  Header for DownloadFragment's RecyclerView, shown via RestoreBannerAdapter
  after a DETECTED reinstall whose automatic backup restore came back empty
  (see DownloadBackupMirror's sentinel-pair detection). Points at the SAF
  restore of the encrypted public mirror surviving in Download/Firedown.

  Sits BELOW the incognito-in-progress header in the ConcatAdapter, but the
  fragment additionally hides this banner entirely while that one is visible
  (vault downloads in flight) — at most one informational banner at a time.
  One-shot: the X dismisses it permanently; any completed restore retires it
  too (state in backup_local.xml — install-local on purpose, so a dismissal
  can never ride a backup into the next reinstall, which is exactly when the
  banner is needed again).
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/list_spacing"
    android:layout_marginTop="@dimen/list_spacing"
    android:layout_marginBottom="@dimen/incognito_banner_margin_bottom"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="?attr/colorSurfaceContainerHighest">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="8dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/restore_banner_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="@null"
            android:src="@drawable/settings_backup_restore_24"
            app:tint="?attr/colorOnSurface"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <TextView
            android:id="@+id/restore_banner_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="@string/restore_banner_title"
            android:textAppearance="?attr/textAppearanceTitleSmall"
            android:textColor="?attr/colorOnSurface"
            app:layout_constraintStart_toEndOf="@id/restore_banner_icon"
            app:layout_constraintEnd_toStartOf="@id/restore_banner_close"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="2dp"
            android:text="@string/restore_banner_subtitle"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="?attr/colorOnSurface"
            android:alpha="0.8"
            app:layout_constraintStart_toEndOf="@id/restore_banner_icon"
            app:layout_constraintEnd_toStartOf="@id/restore_banner_close"
            app:layout_constraintTop_toBottomOf="@id/restore_banner_title" />

        <!-- Dismiss is its own touch target; the rest of the card carries the
             restore tap. -->
        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/restore_banner_close"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/task_dismiss"
            android:src="@drawable/ic_baseline_close_24"
            app:tint="?attr/colorOnSurfaceVariant"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
