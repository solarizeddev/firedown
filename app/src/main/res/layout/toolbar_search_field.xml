<?xml version="1.0" encoding="utf-8"?>
<!--
  The in-toolbar, full-width search field — the same look Downloads/Vault use
  (an EditText that takes over the whole toolbar row, with a trailing clear
  button), extracted so a fragment that borrows an activity toolbar (Cloud
  Backup's files list) can add it as a toolbar child at runtime. Match the
  Downloads/Vault inline copies if you change the metrics.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/search_bar"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:visibility="gone">

    <EditText
        android:id="@+id/search_edit"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:background="@null"
        android:gravity="center_vertical"
        android:hint="@string/search"
        android:imeOptions="actionSearch"
        android:importantForAutofill="no"
        android:inputType="text"
        android:maxLines="1"
        android:textColor="?attr/colorOnSurface"
        android:textColorHint="?attr/colorOnSurfaceVariant"
        android:textSize="18sp" />

    <ImageButton
        android:id="@+id/search_clear"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginEnd="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/abc_searchview_description_clear"
        android:scaleType="center"
        android:src="@drawable/ic_clear_24"
        android:visibility="gone"
        app:tint="?attr/colorOnSurfaceVariant" />

</LinearLayout>
