<?xml version="1.0" encoding="utf-8"?>
<!--
  Info bottom sheet content. The RecyclerView is constraint-driven
  with layout_constraintHeight_default=wrap + a maxHeight so the
  sheet wraps short metadata lists tightly but scrolls inside the
  sheet when a file's description blows past the cap. Without the
  max-height constraint a huge description would size the RV past
  BaseBottomSheetDialogFragment's 640dp sheet cap and the bottom
  items would clip instead of scroll.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <include
        android:id="@+id/drag_handle"
        layout="@layout/dialog_drag_handle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="6dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constrainedHeight="true"
        app:layout_constraintHeight_default="wrap"
        app:layout_constraintHeight_max="560dp"
        app:layout_constraintTop_toBottomOf="@id/drag_handle"
        app:layout_constraintBottom_toTopOf="@id/anchor_view" />

    <View
        android:id="@+id/anchor_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
