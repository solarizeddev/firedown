<?xml version="1.0" encoding="utf-8"?>
<!--
  Home recent-downloads card. Always-visible counterpart to the
  bottom-bar cradle flame's quick-access sheet — sits between the
  onboarding card and the shortcuts grid and surfaces the four most
  recent downloads so a fresh user can see Firedown's actual job
  ('downloader') on the home surface itself, not just behind a tap.

  HomeFragment hides the card whenever the underlying LiveData is
  empty, so a fresh install with zero downloads renders exactly as
  before (onboarding + shortcuts). Rows reuse the same
  DownloadsQuickAccessAdapter as the sheet so both surfaces show
  the same data in the same shape.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/recent_downloads_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:visibility="gone"
    style="@style/Widget.Material3.CardView.Filled"
    app:cardBackgroundColor="@color/md_theme_surfaceContainerLow_highContrast">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="12dp"
        android:paddingBottom="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingEnd="8dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/downloads_quick_access_title"
                android:textAppearance="?attr/textAppearanceTitleMedium"
                android:textColor="?attr/colorOnSurface" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/recent_downloads_view_all"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minHeight="0dp"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp"
                android:text="@string/downloads_quick_access_view_all" />

        </LinearLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recent_downloads_recycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:overScrollMode="never"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
