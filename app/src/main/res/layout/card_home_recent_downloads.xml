<?xml version="1.0" encoding="utf-8"?>
<!--
  Recent-downloads card embedded directly in the home page. Mirrors
  the visual language of DownloadsQuickAccessSheet (same row layout,
  same adapter) so long-pressing the Downloads button and tapping
  the home card land on the same thing — just without the bottom
  sheet wrapper. Lives between the onboarding card and the centred
  brand glyph, and is hidden via the SETTINGS_HOME_SHOW_RECENT_DOWNLOADS
  toggle or when there are no recent downloads.
-->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/recent_downloads_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginTop="8dp"
    android:visibility="gone"
    app:cardBackgroundColor="?attr/colorSurfaceContainerHighest"
    app:cardElevation="0dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="8dp">

        <TextView
            android:id="@+id/recent_downloads_title"
            style="@style/TextAppearance.Material3.TitleMedium"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:paddingTop="12dp"
            android:paddingBottom="4dp"
            android:text="@string/downloads_quick_access_title"
            android:textColor="?attr/colorOnSurface" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recent_downloads_recycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:overScrollMode="never"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/recent_downloads_view_all"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="4dp"
            android:text="@string/downloads_quick_access_view_all" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
