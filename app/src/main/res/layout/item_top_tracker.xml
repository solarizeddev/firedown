<?xml version="1.0" encoding="utf-8"?>
<!--
  Single row of the 'Top trackers' list inside TrackersInfoSheet.
  Count hugs the start (tabular numerics so digit columns align
  down the list), hostname fills the rest with ellipsize=end. No
  background, no ripple — the rows are not tappable in v1 to
  match Brave's pattern.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingVertical="6dp">

    <!-- Count + host share the same typography as the CertDialog
         label/value rows: 13sp monospace at textColorPrimary. Monospace
         makes hostname segments line up vertically across rows and
         keeps the count column intrinsically tabular without
         needing fontFeatureSettings tricks. Host is textIsSelectable
         so users can long-press a row to copy a tracker domain
         (e.g. to add to an external blocklist). -->
    <TextView
        android:id="@+id/top_tracker_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:gravity="end"
        android:minWidth="48dp"
        android:paddingEnd="12dp"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="13sp"
        tools:ignore="RtlSymmetry"
        tools:text="32" />

    <TextView
        android:id="@+id/top_tracker_host"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:fontFamily="monospace"
        android:maxLines="1"
        android:textColor="?android:attr/textColorPrimary"
        android:textIsSelectable="true"
        android:textSize="13sp"
        tools:text="doubleclick.net" />

</LinearLayout>
