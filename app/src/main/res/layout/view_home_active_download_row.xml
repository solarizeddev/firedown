<?xml version="1.0" encoding="utf-8"?>
<!--
  Single row inside the active-download strip. Stacks filename +
  percent on top, progress bar below. HomeFragment inflates one of
  these per in-flight regular download (capped at 3) into the
  strip's rows container. Top padding gives 10dp gap between rows
  and between the first row and the header above.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/active_download_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:textAppearance="?attr/textAppearanceTitleSmall"
            android:textColor="?attr/colorOnPrimaryContainer"
            tools:text="my_video_file_name.mp4" />

        <TextView
            android:id="@+id/active_download_percent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="?attr/colorOnPrimaryContainer"
            tools:text="45%" />

    </LinearLayout>

    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/active_download_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        app:indicatorColor="?attr/colorOnPrimaryContainer"
        app:indicatorTrackGapSize="0dp"
        app:trackCornerRadius="2dp"
        app:trackStopIndicatorSize="0dp" />

</LinearLayout>
